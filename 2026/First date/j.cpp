#include <iostream>
#include <math.h>
#include <set>
#include <vector>

using namespace std;

typedef unsigned long long int ulli;

ulli fact(ulli n)
{
    ulli res = 0;
    return res;
}

int main ()
{
    ulli t = 0;
    vector<ulli> tVec;

    cin >> t;
    tVec.reserve(t);
    
    for (ulli i = 0; i < t; i++)
    {
        ulli n = 0;
        cin >> n;
        
        while ()
    }
    
 
    if (accPe >= ceil(accPro * 1.1))
    {
        cout << "YES" << "\n";
    } else {
        cout << "NO" << "\n";
    }

    return 0;
}
