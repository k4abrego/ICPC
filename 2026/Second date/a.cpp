#include <set>
#include <iostream>
#include <ios>


using namespace std;

long long permutations(int k, int n, const string& s ) {
    multiset<char> sett;
    
    for (char c : s) {
        sett.insert(c);
    }

}

int main() {

    ios::sync_with_stdio(false);
    cin.tie(nullptr);


    int t, n, k;

    cin >> t;

    for (int i = 0; i < t; ++i) {

    }
}